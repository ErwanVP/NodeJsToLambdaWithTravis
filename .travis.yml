language: node_js

sudo: false

node_js:
  - 4.3

# command to install dependencies

# command to run tests
  #npm test

deploy:
  provider: lambda
  function_name: "$FONCTION_NAME"
  role: "$ROLE_NAME"
  handler_name: "index.py"
  access_key_id: "$AWS_ACCESS_KEY"
  secret_access_key: "$AWS_SECRET_ACCESS"
